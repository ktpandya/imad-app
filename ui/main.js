/*var username = document.getElementById('username');
username.onclick.function(result);
{
pool.query(`SELECT * FROM "user" WHERE "username" = ` , username , function (err, result)
{
    if (err)
        {}
    else
    {pool.query(`SELECT * FROM "user" WHERE "password" = ` , password , function (err,result)
    {if (err)
        {res.send('error');}
    else
    {}
    })}}*/